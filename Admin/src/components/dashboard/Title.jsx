import React from "react";

const Title = ({ title }) => {
  return (
    <p className="w-full text-white/70 text-xl font-semibold text-center my-10">
      {title}
    </p>
  );
};

export default Title;
